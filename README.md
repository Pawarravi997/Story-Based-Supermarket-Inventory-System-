# Story-Based-Supermarket-Inventory-System-



SQL practice project on a Supermarket Database — covers DDL (CREATE/ALTER TABLE), DML (INSERT/UPDATE/DELETE), and SELECT queries with WHERE, AND/OR, and comparison operators. Simulates real-world inventory management scenarios (stock updates, pricing changes, city-wise filtering, discount logic).
